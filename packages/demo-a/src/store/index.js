import Vue from 'vue'
import Vuex from 'vuex'

import { name } from "@@/../package.json";
console.log(name, 'globa package.json name')

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
